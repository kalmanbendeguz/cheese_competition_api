//const Joi = require('joi')

const action_validator = (_action_validator) => {

    // this is a wrapper object, if later i want to apply a global action-specific validation rule.

    return _action_validator
}

module.exports = action_validator